# Only needed if the frontend and backend are deployed as separate services
# (e.g. frontend on Vercel, backend on Railway). Leave unset for same-origin deploys.
VITE_API_URL=https://your-backend.up.railway.app
